{# The Smarty version of this hook loaded bootstrap-switch/bootstrap-editable and wired
   click handlers for the visibility switches, the in-place position editor and the delete
   dialog. The Bootstrap 5 markup rendered by module-configuration.html.twig replaces all of
   that with plain links (toggle-online link, up/down position links) and the BoConfirmDialog
   component (which already wires the delete dialog prefill through Stimulus), so no
   additional JavaScript is required here. #}
